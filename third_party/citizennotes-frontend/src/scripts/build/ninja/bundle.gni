# Copyright 2021 The Chromium Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

import("../../../third_party/typescript/typescript.gni")
import("./node.gni")
import("./vars.gni")

declare_args() {
  # If this is enabled, citizennotes build uses esbuild instead of rollup.js to
  # bundle JavaScript files.
  citizennotes_fast_bundle = citizennotes_skip_typecheck

  # If this is enabled, we will emit source maps for the entrypoint bundles.
  # Since we only bundle release builds, this flag can't be used with
  # is_debug = true.
  # Note that this flag works regardless of the citizennotes_skip_typecheck or
  # citizennotes_fast_bundle GN arg. Both rollup.js or ESBuild will emit source
  # maps if this flag is set.
  citizennotes_release_sourcemaps = false
}

assert(!(citizennotes_fast_bundle && is_official_build),
       "Official build should not bundle with esbuild")

# Release source map implies release build.
assert(!citizennotes_release_sourcemaps || !is_debug,
       "Release source maps can only be enabled for release builds")

template("bundle") {
  assert(defined(invoker.entrypoint),
         "You must define the 'entrypoint' for a bundle target")

  if (citizennotes_fast_bundle) {
    node_action(target_name) {
      script = "scripts/build/esbuild.js"
      forward_variables_from(invoker,
                             [
                               "visibility",
                               "deps",
                               "public_deps",
                             ])

      inputs = [
        invoker.entrypoint,
        citizennotes_location_prepend + "scripts/build/citizennotes_plugin.js",
        citizennotes_location_prepend + "scripts/citizennotes_paths.js",
      ]

      _esbuild = citizennotes_location_prepend + "third_party/esbuild/esbuild"
      if (host_os == "win") {
        inputs += [ _esbuild + ".exe" ]
      } else {
        inputs += [ _esbuild ]
      }

      args = [
        rebase_path(invoker.entrypoint, root_build_dir),
        rebase_path(invoker.output_file_location, root_build_dir),
      ]

      if (citizennotes_release_sourcemaps) {
        args += [ "--configSourcemaps" ]
      }

      outputs = [ invoker.output_file_location ]
    }
  } else {
    node_action(target_name) {
      script = "node_modules/rollup/dist/bin/rollup"

      forward_variables_from(invoker,
                             [
                               "visibility",
                               "deps",
                               "public_deps",
                             ])

      inputs = [
        invoker.entrypoint,
        citizennotes_location_prepend + "scripts/build/rollup.config.js",
        citizennotes_location_prepend + "scripts/build/citizennotes_plugin.js",
        citizennotes_location_prepend + "scripts/citizennotes_paths.js",
      ]

      args = [
        # TODO(crbug.com/1098074): We need to hide warnings that are written stderr,
        # as Chromium does not process the returncode of the subprocess correctly
        # and instead looks if `stderr` is empty.
        "--silent",
        "--config",
        rebase_path(
            citizennotes_location_prepend + "scripts/build/rollup.config.js",
            root_build_dir),
        "--input",
        rebase_path(invoker.entrypoint, root_build_dir),
        "--file",
        rebase_path(invoker.output_file_location, root_build_dir),
      ]

      if (!citizennotes_dcheck_always_on) {
        args += [ "--configDCHECK" ]
      }

      if (citizennotes_release_sourcemaps) {
        args += [ "--configSourcemaps" ]
      }

      outputs = [ invoker.output_file_location ]
    }
  }
}
